[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (including /admin/*) work on
# direct load and refresh, not just client-side navigation.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
